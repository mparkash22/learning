points = [
    {'x':2,'y':3},
    {'x':4,'y':1}
]

points.sort(key=lambda i : i['y'])
print(points)


#list comprehentios
#[<Statement to execute> for <variables> in [list ] <conditions> ]
# * in args then extra variable stored as tuple, ** the extra args passed stored as dictionary

